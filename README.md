# SaveFormData

Auto save and restore form data.

## CDN

<script src="https://cdn.jsdelivr.net/gh/USERNAME/save-form-data/dist/save-form-data.min.js"></script>

## Usage

const saver = SaveFormData({
  form_selector:'#form'
});

saver.clear();
saver.reset();